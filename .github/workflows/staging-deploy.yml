name: Staging Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    env:
      STAGING_DEPLOY_WEBHOOK_VALUE: ${{ secrets.STAGING_DEPLOY_WEBHOOK }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci

      - name: Quality Gates
        env:
          BUILD_OUT_DIR: dist
        run: npm run quality

      - name: Trigger Staging Deploy
        if: ${{ env.STAGING_DEPLOY_WEBHOOK_VALUE != '' }}
        run: |
          curl -sS -X POST "$STAGING_DEPLOY_WEBHOOK"
        env:
          STAGING_DEPLOY_WEBHOOK: ${{ env.STAGING_DEPLOY_WEBHOOK_VALUE }}

      - name: Staging Deploy Not Configured
        if: ${{ env.STAGING_DEPLOY_WEBHOOK_VALUE == '' }}
        run: echo "STAGING_DEPLOY_WEBHOOK is not configured. Skipping deploy trigger."
